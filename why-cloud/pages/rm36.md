---
layout: fact
transition: fade-out
---

<div v-motion :initial="{ scale: 0.4, opacity: 0 }" :enter="{ scale: 1, opacity: 1, transition: { type: 'spring', stiffness: 80, delay: 200 } }">

# RM 36

</div>

<div v-motion :initial="{ opacity: 0 }" :enter="{ opacity: 1, transition: { delay: 700, duration: 500 } }" class="text-2xl opacity-70 mt-2">
a full month of that server
</div>

<div v-motion :initial="{ opacity: 0 }" :enter="{ opacity: 1, transition: { delay: 1100, duration: 500 } }" class="mt-8 text-lg opacity-60">
stop it anytime and the meter stops
</div>

<!--
Do the arithmetic out loud: the size we just launched costs about 1 US cent an hour in Singapore, roughly 5 sen. Around 730 hours in a month makes about 8 US dollars, call it RM 36. Put that next to the RM 42,000 quotation, the cooled room and the engineer's salary from the old way.
And it's rent, not a purchase: stop the server and the hourly meter stops; only the disk keeps costing a few ringgit until you delete it. Terminate it and the bill is zero.
For starting out it gets better: a new AWS account comes with free credits (100 US dollars at signup on the current free plan), so tonight's experiments can genuinely cost nothing. The exact pricing page is one search away; happy to show it at the break.
-->
